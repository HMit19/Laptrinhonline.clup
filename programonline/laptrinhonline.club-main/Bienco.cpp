#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie();
	long long a,b;
	while(1)
	{
		cin>>a>>b;
		if(a==0&&b==0)
			break; 
		long long t;
		t=a*b; 
		cout<<t<<endl; 
	} 
}